import React from 'react'

const About = ({ isActive }) => {
  return (
    <section className={`about-section h-screen bg-black ${isActive ? 'active' : ''}`}>
      c
    </section>
  )
}

export default About
