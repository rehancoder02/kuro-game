import React from 'react'

const Protfolio = ({ isActive }) => {
  return (
    <section className={`protfolio-section h-screen bg-blue-500 ${isActive ? 'active' : ''}`}>
      c
    </section>
  )
}

export default Protfolio
